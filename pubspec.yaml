name: zegoliveroom_plugin
description: A flutter plugin that can provide real-time communication and live video broadcast.
version: 1.5.3
homepage: https://zego.im

environment:
  sdk: ">=2.6.0 <3.0.0"
  flutter: ^1.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.zego.zegoliveroomplugin
        pluginClass: ZegoLiveRoomPlugin
      ios:
        pluginClass: ZegoLiveRoomPlugin
